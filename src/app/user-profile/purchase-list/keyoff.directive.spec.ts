import { KeyoffDirective } from './keyoff.directive';

describe('KeyoffDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyoffDirective();
    expect(directive).toBeTruthy();
  });
});
