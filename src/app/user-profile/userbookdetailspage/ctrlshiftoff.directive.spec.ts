import { CtrlshiftoffDirective } from './ctrlshiftoff.directive';

describe('CtrlshiftoffDirective', () => {
  it('should create an instance', () => {
    const directive = new CtrlshiftoffDirective();
    expect(directive).toBeTruthy();
  });
});
