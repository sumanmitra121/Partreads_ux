import { NorightDirective } from './noright.directive';

describe('NorightDirective', () => {
  it('should create an instance', () => {
    const directive = new NorightDirective();
    expect(directive).toBeTruthy();
  });
});
