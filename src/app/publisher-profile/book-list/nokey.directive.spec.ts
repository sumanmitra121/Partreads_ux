import { NokeyDirective } from './nokey.directive';

describe('NokeyDirective', () => {
  it('should create an instance', () => {
    const directive = new NokeyDirective();
    expect(directive).toBeTruthy();
  });
});
